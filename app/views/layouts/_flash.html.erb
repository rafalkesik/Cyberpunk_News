<div id="flash-messages" class="position-sticky top-0">
    <% flash.each do |type, msg| %>
        <%= content_tag(:div, msg, role: 'alert',
                        class: "alert alert-#{type}
                                alert-dismissible fade show") %>
    <% end %>
</div>