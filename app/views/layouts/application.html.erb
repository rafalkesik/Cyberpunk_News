<!DOCTYPE html>
<html>
  <head>
    <title>Cyberpunk News</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
    <% # fontawesome should rather be downloaded and added, so it's not dependent on an external website connection. It's used for upvote icon. %>
    <script src="https://kit.fontawesome.com/f1280871dc.js" crossorigin="anonymous"></script>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render 'layouts/flash' %> 
    <%= render 'layouts/header' %>
    <div class="container content-container bg-body-side-color pt-2 pb-1">
      <%= yield %>
    </div>
    <%= render 'layouts/footer' %>
    <%= debug(params) if Rails.env.development? %>
  </body>
</html>
