<%= form_with(model: resource,
              method: :delete,
              class: 'd-inline-block',
              data: { turbo_confirm: t(:sure?),
                      turbo: true }) do |f| %>
    <%= f.submit t(:delete) %>
<% end %>
