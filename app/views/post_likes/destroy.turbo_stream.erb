<%= turbo_stream.update "post-#{@post.id}-points",
                        (t :points, count: @post.points) %>

<%= turbo_stream.update "post-#{@post.id}-like",
                        partial: 'posts/like_form',
                        locals: { post: @post } %>
